{
  "name": "tree-sitter-razor",
  "version": "0.1.1",
  "description": "Razor grammar for tree-sitter",
  "repository": "https://github.com/anortham/tree-sitter-razor",
  "license": "MIT",
  "author": {
    "name": "Alan Northam",
    "email": "anortham@gmail.com"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "main": "bindings/node",
  "types": "bindings/node",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "razor"
  ],
  "files": [
    "grammar.js",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**",
    "*.wasm"
  ],
  "dependencies": {
    "node-addon-api": "^8.2.2",
    "node-gyp-build": "^4.8.4",
    "tree-sitter-c-sharp": "0.23.5"
  },
  "devDependencies": {
    "prebuildify": "^6.0.1",
    "tree-sitter": "^0.25.0",
    "tree-sitter-cli": "0.26.11"
  },
  "peerDependencies": {
    "tree-sitter": "^0.25.0"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "scripts": {
    "install": "node-gyp-build",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground",
    "test": "node --test bindings/node/*_test.js test/contracts/*.test.mjs",
    "prebuildify": "prebuildify --napi --strip"
  },
  "tree-sitter": [
    {
      "scope": "source.razor",
      "injection-regex": "^razor$"
    }
  ]
}
